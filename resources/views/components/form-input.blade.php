<div class="md:col-span-5 mb-4">
    <label for="full_name">{{ $label }}</label>
    <input type="{{ $type }}" name="{{ $name }}" id="full_name" class="h-10 border mt-1 rounded px-4 w-full bg-gray-50" value="" placeholder="{{ $placeholder }}"/>
</div>